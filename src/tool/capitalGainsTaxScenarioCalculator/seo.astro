---
import { SEORenderer } from '@jjlmoya/utils-shared';
import { capitalGainsTaxScenarioCalculator } from './entry';
import type { KnownLocale } from '../../types';

interface Props { locale?: KnownLocale; }
const { locale = 'en' } = Astro.props;
const content = await capitalGainsTaxScenarioCalculator.i18n[locale]?.() ?? await capitalGainsTaxScenarioCalculator.i18n.en?.();
---

{content?.seo && <SEORenderer content={{ locale, sections: content.seo }} />}
